# Configuração do GitHub Pages.
# Mantém fora do site publicado tudo que é só de bastidor: os fontes das
# imagens e o README. O antigo .htaccess foi para build/ — ele só teria
# efeito num servidor Apache, caso o site volte um dia para a Hostinger.
exclude:
  - build
  - README.md
  - .gitignore
